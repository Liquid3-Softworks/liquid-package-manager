import { createTheme } from "@mui/material";

const mainTheme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
          main: '#121212',
        },
      },
})

export default mainTheme